<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#050505">
  <title>{%- if request.page_type == 'index' -%}{{ shop.name }} — {{ shop.description | default: 'Graphic tees inspired by the culture everyone is watching' }}{%- else -%}{{ page_title }} — {{ shop.name }}{%- endif -%}</title>
  {%- if page_description -%}<meta name="description" content="{{ page_description | escape }}">{%- elsif request.page_type == 'index' -%}<meta name="description" content="{{ shop.description | default: 'CYCL — limited-run graphic tees inspired by the movies, shows, and moments everyone is talking about. Free shipping on every order.' | escape }}">{%- endif -%}
  <link rel="canonical" href="{{ canonical_url }}">

  {%- comment -%} Open Graph / social sharing {%- endcomment -%}
  <meta property="og:site_name" content="{{ shop.name }}">
  <meta property="og:type" content="{% if request.page_type == 'product' %}product{% else %}website{% endif %}">
  <meta property="og:title" content="{% if request.page_type == 'index' %}{{ shop.name }}{% else %}{{ page_title }}{% endif %}">
  <meta property="og:url" content="{{ canonical_url }}">
  {%- if page_description -%}<meta property="og:description" content="{{ page_description | escape }}">{%- endif -%}
  {%- if request.page_type == 'product' and product.featured_image -%}
    <meta property="og:image" content="https:{{ product.featured_image | image_url: width: 1200 }}">
  {%- elsif page_image -%}
    <meta property="og:image" content="https:{{ page_image | image_url: width: 1200 }}">
  {%- endif -%}
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{% if request.page_type == 'index' %}{{ shop.name }}{% else %}{{ page_title }}{% endif %}">
  {%- if page_description -%}<meta name="twitter:description" content="{{ page_description | escape }}">{%- endif -%}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" media="print" onload="this.media='all'">
  <noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap"></noscript>
  {{ 'theme.css' | asset_url | stylesheet_tag }}
  {{ content_for_header }}
  <script>window.Shopify=window.Shopify||{};window.Shopify.currency={active:'{{ cart.currency.iso_code }}'};</script>
  <script>
  !function(f,b,e,v,n,t,s)
  {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
  n.callMethod.apply(n,arguments):n.queue.push(arguments)};
  if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
  n.queue=[];t=b.createElement(e);t.async=!0;
  t.src=v;s=b.getElementsByTagName(e)[0];
  s.parentNode.insertBefore(t,s)}(window, document,'script',
  'https://connect.facebook.net/en_US/fbevents.js');
  fbq('init', '1314861717491311');
  fbq('track', 'PageView');
  </script>
  <noscript><img height="1" width="1" style="display:none"
  src="https://www.facebook.com/tr?id=1314861717491311&ev=PageView&noscript=1"
  /></noscript>
</head>
<body class="page-type--{{ request.page_type }}{% if customer %} customer-logged-in{% endif %}">
  {%- sections 'announcement-bar' -%}
  {%- section 'header' -%}
  <div class="cart-overlay" data-close-cart aria-hidden="true"></div>
  {%- section 'cart-drawer' -%}
  <div class="mobile-nav-overlay" data-close-menu aria-hidden="true"></div>
  <nav class="mobile-nav" role="navigation" aria-label="Mobile navigation">
    <div class="mobile-nav__top">
      <span class="mobile-nav__tag">// MENU</span>
      <button class="mobile-nav__close" data-close-menu aria-label="Close menu" type="button">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"><line x1="6" y1="6" x2="18" y2="18"/><line x1="18" y1="6" x2="6" y2="18"/></svg>
      </button>
    </div>
    <div class="mobile-nav__links">
      {%- for link in linklists['main-menu'].links -%}
        <a class="mobile-nav__link" href="{{ link.url }}" style="--i: {{ forloop.index0 }};">
          <span class="mobile-nav__index">{{ forloop.index | prepend: '0' | slice: -2, 2 }}</span>
          <span class="mobile-nav__text">{{ link.title }}</span>
          <svg class="mobile-nav__arrow" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
        </a>
      {%- endfor -%}
    </div>
    <div class="mobile-nav__footer">
      {%- if shop.customer_accounts_enabled -%}
        <a class="mobile-nav__util" href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}">{%- if customer -%}Account{%- else -%}Sign in{%- endif -%}</a>
      {%- endif -%}
      <a class="mobile-nav__util" href="{{ routes.search_url }}">Search</a>
    </div>
  </nav>
  <main id="MainContent" role="main" tabindex="-1">{{ content_for_layout }}</main>
  {%- section 'footer' -%}
  <div class="toast" role="status" aria-live="polite"></div>
  <script src="{{ 'theme.js' | asset_url }}" defer></script>
</body>
</html>
